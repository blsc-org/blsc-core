# normalized/ — Cleaned and Normalized Texts

This folder contains cleaned, normalized versions of the raw source texts.
Allowed operations include:
- spelling normalization,
- OCR correction,
- removal of duplicate line breaks,
- structure-preserving cleanup.

Each file must have a corresponding raw source in `raw/` and metadata in `metadata/`.
